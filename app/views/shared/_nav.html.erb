<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">Booking <span class="text-primary">Home</span></a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to 'Inicio', root_path, class: 'nav-link' %>
        </li>
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to 'Propiedades', user_properties_path, class: 'nav-link' %>
          </li>
        <% end %>
      </ul>

      <% if user_signed_in? %>
        <p class="text-light mt-3 mx-3">Bienvenido: <%= current_user.email %></p>
        <%= link_to 'Cerrar sesión', destroy_user_session_path, data: { turbo_method: :delete }, class: 'btn btn-primary' %>

      <% else %>
        <%= link_to 'Iniciar sesión', new_user_session_path, class: 'btn btn-primary' %>
      <% end %>
    </div>
  </div>
</nav>